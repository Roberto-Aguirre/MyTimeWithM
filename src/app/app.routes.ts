import { Routes } from '@angular/router';
import { PrincipalPageComponent } from './landing/principal-page/principal-page';

export const routes: Routes = [
    { path: '', component: PrincipalPageComponent }
];
